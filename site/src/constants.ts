// UI-only constants for the catalog site. Icon taxonomy (variants, categories,
// labels) lives in ../../shared/icon-taxonomy — import those directly from there.

export const DEFAULT_ICON_SIZE = 32
export const ICON_SIZE_MIN = 20
export const ICON_SIZE_MAX = 48
export const COPY_STATUS_DURATION_MS = 1800
